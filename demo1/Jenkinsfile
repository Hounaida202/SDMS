pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/Hounaida202/SDMS.git'
            }
        }

        stage('Test Maven') {
            steps {
                dir('demo1') {
                    // Vérifie que Maven fonctionne
                    bat 'mvn --version'

                    // Test simple
                    bat 'mvn clean compile'
                }
            }
        }
    }
}